rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open Digital Public Good advisory records
    match /agro_advisories/{advisoryId} {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
